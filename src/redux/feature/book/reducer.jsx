import { LOADED } from "./actionType";



const reducer = (state , action) => {
    switch (key) {
        case LOADED:
            return action.payload;
    
        default:
            return state
    }
}

export default reducer;